{
  "name": "@ssgw/next-chapter",
  "version": "0.1.15",
  "description": "A link component to go to the next chapter",
  "main": "dist/index.js",
  "scripts": {
    "build": "node ../../scripts/build.js",
    "clean": "rm -rf dist node_modules",
    "publish": "npm publish || echo 'could not publish, skipping'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "R/GA",
  "license": "UNLICENSED",
  "dependencies": {
    "@ssgw/utils": "^0.2.4",
    "classnames": "^2.2.6",
    "prop-types": "^15.6.2"
  },
  "peerDependencies": {
    "node-sass": ">=4.10.0",
    "react": ">=16.6.0",
    "react-dom": ">=16.6.0"
  }
}
