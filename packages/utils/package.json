{
  "name": "@ssgw/utils",
  "version": "0.2.8",
  "description": "Common JS and Sass code used across chapter-componets",
  "main": "dist/index.js",
  "scripts": {
    "build": "node ../../scripts/build.js",
    "clean": "rm -rf dist node_modules",
    "publish": "npm publish || echo 'could not publish, skipping'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Hettler",
  "license": "UNLICENSED",
  "peerDependencies": {
    "node-sass": ">=4.10.0",
    "react": ">=16.6.0",
    "react-dom": ">=16.6.0"
  },
  "dependencies": {
    "react-matchmedia-connect": "^0.2.1"
  }
}
