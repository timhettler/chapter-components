{
  "name": "@ssgw/begin-button",
  "version": "0.1.3",
  "description": "The button used to start a Samsung Within experience",
  "main": "dist/index.js",
  "scripts": {
    "babel": "../../node_modules/.bin/babel --root-mode upward src -d dist --source-maps --ignore \"src/**/*.stories.js\"",
    "build": "../../node_modules/.bin/webpack ./src/index.js --o ./dist/index.js --config ../../config/webpack.config.js",
    "clean": "rm -rf dist node_modules",
    "publish": "npm publish || echo 'could not publish, skipping'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Hettler",
  "license": "UNLICENSED",
  "dependencies": {
    "classnames": "^2.2.6",
    "prop-types": "^15.6.2"
  },
  "peerDependencies": {
    "react": "16.6.x",
    "react-dom": "16.6.x"
  }
}
