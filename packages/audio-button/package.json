{
  "name": "@ssgw/audio-button",
  "version": "1.0.0",
  "description": "A component that controls audio sound",
  "main": "dist/index.js",
  "scripts": {
    "build": "node ../../scripts/build.js",
    "clean": "rm -rf dist node_modules",
    "publish": "npm publish || echo 'could not publish, skipping'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "R/GA",
  "license": "UNLICENSED",
  "dependencies": {
    "@ssgw/button": "^1.0.0",
    "@ssgw/utils": "^1.0.0",
    "classnames": "^2.2.6",
    "prop-types": "^15.7.2"
  },
  "peerDependencies": {
    "node-sass": ">=4.10.0",
    "react": ">=16.6.0",
    "react-dom": ">=16.6.0"
  }
}
